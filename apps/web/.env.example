# Public frontend URL for metadata and canonical links.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Keep empty for root deployment (recommended for Netlify).
NEXT_PUBLIC_APP_BASE_PATH=

# Required in production: the backend origin used by the browser.
# Local default should point to the FastAPI app.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
