export function AgentActivityPanel() {
  return (
    <section className="card">
      <p className="section-title">Agent Activity</p>
      <p className="text-sm muted">Specialist agent dispatches and outcomes.</p>
      <p className="text-xs muted">
        TODO: Stream live run events from backend traces.
      </p>
    </section>
  );
}
